<% content_for :title, "発電所を編集 - #{@site.name}" %>

<div class="page-header">
  <h2 class="page-title">⚙ 発電所を編集</h2>
  <%= link_to "← 一覧に戻る", sites_path, class: "btn btn-secondary" %>
</div>

<div class="p-6 max-w-lg">
  <div class="card p-6">
    <%= render "form", site: @site, url: site_path(@site), method: :patch %>
  </div>
</div>